# 项目总结文档

## 项目名称

**中文**：基于 YOLO26 与 ByteTrack 的智能驾驶道路参与者检测与风险预警系统

**英文**：YOLO26-Based Road User Detection and Risk Warning System with ByteTrack

---

## 项目简介

本项目面向智能驾驶前视道路场景，基于 YOLO26 实现车辆、行人、自行车、摩托车等道路参与者检测；结合图像空间风险区域、目标框高度比例和 ByteTrack 多目标跟踪，实现车辆与弱势交通参与者（VRU）风险提示；进一步通过 Track ID 累计风险帧数过滤短暂误检，并通过轨迹线可视化展示目标运动过程。项目采用阶段化开发流程，使用 Git 进行版本管理，README 包含效果图展示。

---

## 核心功能

- YOLO26 道路目标检测（car / bus / truck / person / bicycle / motorcycle 等）
- 车辆风险区域预警（前方梯形区域 + 近距视觉约束）
- VRU 弱势交通参与者风险提示（独立阈值、视觉区分品红色标记）
- 风险阈值校准（车辆 0.12 / VRU 0.10，经实景视频验证）
- ByteTrack 多目标跟踪（稳定 Track ID 分配）
- Track ID 显示（绿色框 + `class_name ID:#`）
- 基于 Track ID 的稳定风险过滤（累计 >= 5 帧触发 STABLE 预警）
- 目标运动轨迹可视化（最近 30 帧底边中心点轨迹，风险目标区分颜色）
- 输出视频与统计信息生成（帧数、风险帧数、占比、ID 持续帧数）

---

## 技术路线

```
Input Video
    → YOLO26 Detection          # 逐帧检测道路参与者
    → ByteTrack Tracking        # 为每个目标分配稳定 Track ID
    → Risk Zone Judgment        # 判断目标是否落入前方风险梯形区域
    → Track-based Stable Risk Filtering  # 同一 ID 累计 >= 5 帧才触发稳定预警
    → Trajectory Visualization  # 绘制目标最近 30 帧运动轨迹
    → Output Video              # 输出带标注视频 + 统计信息
```

**各步骤说明**：

1. **YOLO26 Detection** — 使用 Ultralytics YOLO26 nano 预训练权重，在 CPU 上完成逐帧推理，检测道路场景中的车辆、行人、自行车、摩托车等目标。

2. **ByteTrack Tracking** — 通过 `model.track(persist=True)` 接口集成 ByteTrack 多目标跟踪器，为每个检测目标分配跨帧稳定的 Track ID，解决单帧检测的 ID 缺失问题。

3. **Risk Zone Judgment** — 在图像空间中定义前方梯形风险区域（自适应任意分辨率）。对每个目标取检测框底边中心点作为地面近似落点，使用射线法判断是否落入风险梯形。同时引入 bbox 高度比例约束（检测框高度 / 画面高度）替代真实距离判断，区分远近目标。

4. **Track-based Stable Risk Filtering** — 利用 ByteTrack 提供的 Track ID，对每个 ID 累计其在风险区出现的帧数。只有当同一 ID 累计 >= 5 帧时，才触发稳定风险预警（STABLE RISK VEHICLE / STABLE RISK VRU），有效过滤单帧误检和 ByteTrack 短暂 ID 碎片导致的虚假报警。

5. **Trajectory Visualization** — 对每个 Track ID 维护最近 30 帧的底边中心点位置队列（deque），使用 OpenCV polylines 在视频帧上绘制运动轨迹。风险目标轨迹使用偏红/偏品红色，普通目标使用浅青绿色，增强目标运动过程的可解释性。

6. **Output Video** — 将标注后的帧合成为 MP4 视频输出，并在终端打印完整统计：总帧数、原始/稳定风险帧数、风险占比、各 Track ID 持续帧数等。

---

## 风险判断规则

### 车辆风险

- **类别**：car / bus / truck
- **条件**：
  1. 检测框底边中心点落入前方风险梯形区域（射线法）
  2. bbox 高度 / 画面高度 >= 0.12（过滤远处小车）

### VRU 风险

- **类别**：person / bicycle / motorcycle
- **条件**：
  1. 检测框底边中心点落入前方风险梯形区域（射线法）
  2. bbox 高度 / 画面高度 >= 0.10（过滤极远小目标，经实景视频校准）

### 稳定风险

- 同一 Track ID 累计处于风险区的帧数 >= **5** 帧，才触发稳定风险预警
- 标签显示为 `STABLE RISK VEHICLE` / `STABLE RISK VRU`
- 横幅优先显示：`STABLE WARNING: VEHICLE RISK` / `STABLE WARNING: VRU RISK`

### 轨迹线

- 轨迹点：检测框底边中心点 `((x1+x2)/2, y2)`
- 历史长度：每个 Track ID 保留最近 **30** 帧轨迹点
- 颜色：普通目标浅青绿 / 车辆风险偏红 / VRU 风险偏品红

---

## 测试结果摘要

### road_drive_01.mp4（快速路跟车场景）

| 指标 | 数值 |
|------|------|
| 总帧数 | 722 |
| 车辆原始风险帧数 | 502 (69.5%) |
| 稳定车辆风险帧数 | 498 (69.0%) |
| 唯一 Track ID 数 | 1 |
| 稳定车辆 ID | ID 1: 502 帧（前 4 帧缓冲后稳定） |

### road_city_vru_01.mp4（城市道路 / 行人 / 自行车场景）

| 指标 | 数值 |
|------|------|
| 总帧数 | 1800 |
| 原始 VRU 风险帧数 | 1724 (95.8%) |
| 稳定 VRU 风险帧数 | 1687 (93.7%) |
| 唯一 Track ID 数 | 316 |
| 稳定 VRU ID 数 | 180（136 个短命 ID 被稳定过滤排除） |
| Top 稳定 VRU ID | ID 2253: 290 帧, ID 618: 256 帧, ID 1801: 248 帧 |

---

## 项目亮点

- **完整视频级流程**：不是简单调用 YOLO API，而是构建了检测 → 跟踪 → 空间规则判定 → 持续风险过滤 → 轨迹可视化 → 视频输出的端到端流程
- **多组件协同**：将 YOLO26 检测、ByteTrack 跟踪、图像空间风险区域、bbox 尺度约束和 Track ID 持续过滤组合为统一的风险预警系统
- **时序稳定性**：通过 Track ID 累计风险帧数降低单帧误检影响，减少 ByteTrack ID 碎片化导致的虚假报警
- **双类风险区分**：车辆风险（红色 STABLE RISK VEHICLE）与 VRU 风险（品红色 STABLE RISK VRU）独立判定、独立视觉标记
- **可解释性增强**：轨迹线可视化展示目标的底边中心点运动历史，风险与非风险目标颜色区分，方便观察和调试
- **工程化习惯**：使用 Git 进行阶段化开发记录（9 个阶段，15+ commits），PROJECT_LOG.md 完整追溯每步决策，README 有效果图展示
- **纯 CPU 可运行**：使用 YOLO26 nano 权重，无需 GPU，适合快速验证和演示

---

## 当前局限

- 未做真实距离估计和相机标定，风险判定基于图像空间（bbox 高度比 + 梯形区域），非物理距离
- 风险区域和阈值依赖摄像头视角，切换摄像头或安装位置需重新标定
- 未做 TTC（Time-to-Collision）等物理碰撞时间计算
- 人群密集场景下 ByteTrack 可能出现 ID 碎片化（短命 ID 较多），需要稳定过滤弥补
- 目前未接入车道线检测和可行驶区域分割，无法区分本车道与相邻车道
- 暂未做 GUI，当前为命令行脚本
- 测试视频仅 2 段，覆盖场景有限
- 高预警率瓶颈在风险梯形区域形状（城市场景 93.7%），而非代码逻辑错误

---

## 后续改进方向

- 轨迹线淡入淡出效果，增强视觉层次感
- 基于滑动窗口的预警迟滞机制，减少临界帧闪烁
- 车道线检测，区分本车道与邻近车道
- 可行驶区域分割，更精准定义风险区域
- 轻量 GUI（如基于 OpenCV HighGUI 或 Streamlit）
- 更多复杂道路视频测试（夜间、雨天、交叉口等）
- 可选：模型微调，适配特定场景或摄像头
- 可选：TTC 估计，基于 Track ID 的帧间位移估算碰撞时间

---

## 项目结构

```
├── assets/                                    # README 展示图片
├── data/test_videos/                          # 测试视频（不入库）
├── scripts/
│   ├── predict_video.py                       # 逐帧检测 + 风险预警
│   ├── track_video.py                         # ByteTrack 跟踪 + 轨迹线 + 稳定风险
│   ├── extract_frames.py                      # 帧抽取工具
│   └── analyze_samples.py                     # 抽样帧分析工具
├── utils/
│   ├── __init__.py
│   └── warning_utils.py                       # 风险区域、双类预警判定、绘制工具
├── outputs/videos/                            # 输出视频（不入库）
├── .gitignore
├── README.md                                  # 项目说明 + 效果展示
├── PROJECT_LOG.md                             # 阶段化开发记录
└── PROJECT_SUMMARY.md                         # 本文档：项目总结 + 简历 + 面试稿
```

---

## Resume Version

### 精简版（适合简历一条项目经历，3–5 行）

> **基于 YOLO26 与 ByteTrack 的智能驾驶道路参与者检测与风险预警系统**
>
> 基于 YOLO26 实现车辆、行人、自行车、摩托车等道路目标检测，结合图像空间风险区域与 bbox 尺度约束设计车辆/VRU 风险预警逻辑；引入 ByteTrack 实现多目标跟踪和 Track ID 标注，并通过累计风险帧数（>= 5 帧）过滤短暂误检；完成轨迹线可视化、稳定风险区分、输出视频生成与 GitHub 效果展示。

### 详细版（适合面试项目介绍，200–300 字）

> 本项目是一个面向智能驾驶前视场景的道路目标检测与风险预警系统。我选择 YOLO26 nano 作为检测底座，因为它在 CPU 上即可实时推理，适合快速验证。在基础检测之上，我自己设计了前方风险梯形区域的判定逻辑——在图像空间中定义一个自适应分辨率的梯形区域来模拟本车前方道路范围，结合检测框底边中心点（地面近似落点）和射线法判断目标是否在风险区内。为了过滤远处无关目标，我引入了 bbox 高度比约束（车辆 >= 0.12，行人 >= 0.10），这是一种轻量的近距视觉约束，无需相机标定即可区分远近。
>
> 进一步，我集成了 ByteTrack 多目标跟踪器，为每个目标分配稳定的跨帧 Track ID。利用 Track ID，我实现了稳定风险过滤——只有当同一目标连续/累计出现在风险区 5 帧以上时，才触发稳定风险预警，有效减少了单帧误检和跟踪器 ID 碎片化导致的虚假报警。最后加入了轨迹线可视化，展示每个目标最近 30 帧的底边中心点运动路径。
>
> 项目在两个实景视频上完成验证：跟车场景中单辆车被稳定追踪 502 帧；城市 VRU 场景中 316 个 ID 中的 180 个通过稳定过滤。目前局限主要在于未做真实距离估计和车道线分割，后续可基于 Track ID 的帧间位移引入 TTC 估计。完整代码已在 GitHub 开源，README 包含效果图展示。

---

## Interview Script

> （以下为 2–3 分钟面试讲解稿，语气自然，可直接用于面试）

**项目背景**（30 秒）

这个项目是我独立完成的一个智能驾驶方向的目标检测与风险预警系统。核心想法是：给车载前视摄像头拍到的视频，自动检测出道路上的车辆、行人、自行车，判断哪些目标可能对本车构成风险，然后在视频里标注出来。我从零搭建了整个流程，没有用现成的自动驾驶框架。

**技术路线**（40 秒）

整体流程是六步：输入视频 → YOLO26 检测 → ByteTrack 跟踪 → 空间风险判断 → 稳定风险过滤 → 轨迹可视化 → 输出视频。我用的是 YOLO26 nano 预训练权重，纯 CPU 环境跑，不需要 GPU。检测出目标后，通过 ByteTrack 给每个目标分配一个稳定的 Track ID，这样同一辆车在不同帧里能被识别为同一个 ID。

**核心难点**（60 秒）

我认为这个项目里比较关键的设计有两个。第一个是风险判断规则：我在图像空间里定义了一个梯形区域来模拟本车前方的道路范围，然后判断每个目标检测框的底边中心点——这个点近似目标在地面上的位置——是否落入梯形内。同时用 bbox 的高度占画面高度的比例来做近距约束，替代了真实距离估计。车辆阈值设 0.12，VRU 行人阈值设 0.10，这个是经过两段实景视频来回校准的。

第二个是稳定风险过滤：单帧检测容易有误检和漏检，ByteTrack 在密集场景下也会产生很多只出现一两帧的短命 ID。我让每个 Track ID 在风险区里累积帧数，只有到了 5 帧以上才触发"稳定风险"预警，相当于给预警加了一个时间上的确认机制。

**测试结果**（30 秒）

我在两段视频上做了验证。一段是快速路跟车场景，722 帧，一辆车被稳定追踪了 502 帧，稳定过滤只减掉了前 4 帧的缓冲期。另一段是城市行人/自行车场景，1800 帧，316 个唯一 Track ID 中有 180 个通过稳定过滤，136 个短命 ID 被过滤掉。原始预警率 95.8%，稳定后 93.7%。

**局限和后续**（20 秒）

当前主要局限是没有做真实距离估计和相机标定，风险区域依赖摄像头视角。后续我想引入车道线检测来区分本车道和相邻车道，以及基于 Track ID 的帧间位移做 TTC 碰撞时间估算。代码已经在 GitHub 上开源，README 里有完整的演示效果图。

---

## Interview Q&A

### 1. 为什么选择 YOLO26？

YOLO26 是 Ultralytics 最新的 YOLO 系列之一，nano 版本模型小、推理快，在 CPU 上就能达到 28–50ms/帧的推理速度，适合快速验证和演示。它的预训练权重覆盖了道路场景常见的 car、person、bicycle、motorcycle 等 80 个类别，开箱即用，不需要额外微调就能在道路场景中取得不错的效果。而且 Ultralytics 的 Python API 设计简洁，方便在工程脚本中集成。

### 2. 为什么还要用 ByteTrack？

YOLO 的检测是逐帧独立的，同一辆车在相邻两帧会被当成完全无关的目标。ByteTrack 通过运动特征（Kalman 滤波）和外观特征匹配，把跨帧的同一目标关联起来，分配一个稳定的 Track ID。有了 Track ID 之后，我就可以做时序相关的分析——比如统计某个目标在风险区里持续了多少帧，实现稳定风险过滤。这是单纯逐帧检测做不到的。

### 3. 风险区域为什么用梯形？

梯形模拟了本车前方道路在图像中的投影形状——近处宽、远处窄。底面（下边界）覆盖画面底部，对应本车前方的近处区域；顶面（上边界）较窄，对应远处的道路消失点方向。用梯形而不是矩形，是因为道路在透视投影下本身就是梯形。四个顶点的坐标使用相对值（相对于画面宽高的比例），可以自适应不同分辨率的视频。

### 4. 为什么用 bbox height ratio 判断远近？

在单目视觉中没有深度信息，但检测框的高度和目标到摄像头的距离有近似的反比关系——同样大小的物体，越近在画面中越高。用 bbox 高度除以画面高度得到一个归一化的比例，可以作为"目标离摄像头有多近"的近似指标。这个方法不需要相机标定，实现简单，在固定的摄像头视角下效果足够用。

### 5. 为什么车辆阈值是 0.12，VRU 是 0.10？

两个阈值都是通过实景视频校准得到的。车辆阈值 0.12 较严格，因为车辆本身尺寸大，在画面中占据的高度比通常也大，用 0.12 可以有效过滤远处的小车。VRU 阈值 0.10 较宽松，因为行人和自行车天然目标尺寸较小，即使较近的行人 bbox 高度比也可能不到 0.12。最终这个值是先设 0.06（几乎不过滤），然后用城市行人视频逐次上调到 0.10 后确认的——只过滤了约 5% 的最远小目标，不影响近处行人触发。

### 6. Track ID 稳定风险过滤解决了什么问题？

解决了两个问题。第一是单帧误检——某一帧 YOLO 把背景误检为目标，或者 ByteTrack 给了一个错误匹配的 ID，如果只存在一两帧就会被 5 帧阈值过滤掉。第二是 ByteTrack 在密集场景下的 ID 碎片化——同一个行人可能因为遮挡、检测丢失等原因被拆成多个只出现 1-3 帧的短命 ID，这些短命 ID 在稳定过滤后不再产生虚假预警。在 VRU 城市场景中，316 个唯一 ID 中有 136 个因不足 5 帧被过滤。

### 7. 这个项目和真正的自动驾驶系统有什么差距？

差距主要在几个层面。第一，感知层面我只用了目标检测和跟踪，没有做深度估计、车道线检测、可行驶区域分割、交通标志识别等。第二，风险判断基于图像空间规则而非物理距离和 TTC，真正的系统需要毫米波雷达或双目视觉来做精确测距。第三，我只有感知和预警，没有规划和控制模块——真正的系统需要根据风险等级做出减速、变道等决策。第四，我的系统没有做传感器融合、冗余设计和功能安全认证。不过作为个人项目，它展示了从检测到预警的完整流程，是理解自动驾驶感知层的一个很好的切入点。

### 8. 为什么没有做真实测距？

真实测距需要相机标定（内参）和地面平面假设（外参），或者需要双目摄像头/深度传感器。在单目 RGB 摄像头且没有标定数据的情况下，bbox 高度比是一个轻量且有效的替代方案。如果需要引入真实距离，后续可以基于标准的 pinhole 相机模型和已知的摄像头安装高度来估算。

### 9. 行人密集场景下有什么问题？

有两个主要问题。第一，ByteTrack 在密集人群场景下容易产生 ID Switch（目标交叉时 ID 互换）和 ID 碎片化（同一行人因遮挡被分配多个短命 ID），这个问题在 VRU 城市视频中体现为 316 个 ID 中大量仅出现 1–2 帧的短命 ID。第二，密集场景下几乎所有行人都可能落入风险梯形区域，导致高预警率（93.7%），这在工程上削弱了预警的实际意义。解决方向是缩窄风险梯形区域，或者结合车道线信息只关注道路区域内的行人。

### 10. 后续如何改进？

短期改进：加入滑动窗口/迟滞阈值减少临界帧闪烁；缩窄风险梯形区域降低高预警率。中期改进：接入车道线检测，区分本车道与相邻车道；基于 Track ID 帧间位移估算 TTC。长期改进：多场景视频测试集构建；模型微调；GUI 可视化。

### 11. 如果摄像头视角变化，规则会不会失效？

会有影响。风险梯形区域的四个顶点坐标是相对于画面宽高的固定比例，如果摄像头安装位置、俯仰角或 FOV 变化，梯形可能不再准确覆盖前方道路区域。但因为是相对坐标设计，相同摄像头位置的不同分辨率视频可以自适应。如果需要适配不同安装位置，需要重新标定梯形坐标和 bbox 高度比阈值。

### 12. 这个项目最大的亮点是什么？

最大亮点是**不是简单调用 API**。如果我直接 `yolo predict` 一行命令跑完就说是项目，那没有任何技术含量。我从头设计了空间风险判定规则（梯形区域 + 射线法 + bbox 高度比），合理复用了开源模型但自己做了上层逻辑。引入了 ByteTrack 不只是"能用"，而是利用 Track ID 做了稳定风险过滤这个时序确认机制。轨迹线也不是简单画点，而是用 deque 维护历史、按风险等级区分颜色。整个项目的每个阶段都有明确的工程目标、测试验证和 Git 记录，可以清晰地看到每次改进的原因和效果。

---

## 开发时间线

| 阶段 | 日期 | 内容 | Commit |
|------|------|------|--------|
| 1 | 05-15 | 项目立项 + YOLO26 基础推理 | — |
| 2 | 05-15 | 工程化逐帧视频推理脚本 | `c27f83c` |
| 3 | 05-15 | 前方车辆风险区域与预警 | `e1d6ee5` |
| 3.5 | 05-16 | 风险预警逻辑校准（bbox 高度比） | `835d523` |
| 4 | 05-16 | VRU 弱势交通参与者风险提示 | `eac70f5` |
| 4.5 | 06-04 | VRU 实景验证 | — |
| 4.6 | 06-04 | VRU 阈值校准（0.06→0.10） | `c88f686` |
| 4.7 | 06-04 | 测试样例说明整理 | `4905df9` |
| 5 | 06-04 | ByteTrack 多目标跟踪 | `df0bea8` |
| 6 | 06-04 | 基于 Track ID 的稳定风险过滤 | `5776e4d` |
| 7 | 06-04 | 项目展示与 README 强化 | `d80596f` |
| 8 | 06-04 | 轨迹线绘制与运动轨迹可视化 | `3705716` |
| 9A | 06-04 | 轨迹线版本截图强化 README | `a8f17b8` |
| 9B | 06-04 | 最终项目总结 + 简历 + 面试稿 | — |

---

*本文档最后一次更新：2026-06-04*
